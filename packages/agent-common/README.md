# Vaeloom Agent Common

Core agent execution primitives:

- BaseAgent class bound to canonical manifest and policy engine
- ReAct step execution (Thought -> Action -> Observation)
- Structured SSE event emission
- Cancellation token and timeout handlers
