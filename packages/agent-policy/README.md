# Vaeloom Agent Policy

Declarative policy engine enforcing agent.yaml manifests:

- Tool authorization & allowlist/denylist
- Memory scope filtering (read/write/denied)
- Delegation depth limiter
- Budget (tokens, steps, USD) enforcement
- Human-in-the-loop approval triggers
