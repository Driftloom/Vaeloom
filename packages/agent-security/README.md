# Vaeloom Agent Security

Zero-trust security primitives:

- Strict SecurityContext (workspace, tenant, non-null user)
- Untrusted input prompt fencing with XML boundaries
- SSRF guard & IP validation
- AES-256-GCM authenticated encryption
- PII and token scrubbing
