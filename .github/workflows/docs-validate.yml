name: Docs Validation

on:
  pull_request:
    paths:
      - 'specs/**'
      - 'docs/**'
      - 'evidence/**'

jobs:
  validate:
    name: Validate Documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Vale Lint
        uses: errata-ai/vale-action@v2
        with:
          files: 'specs docs'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Markdownlint
        uses: DavidAnson/markdownlint-cli2-action@v16
        with:
          globs: |
            specs/**/*.md
            docs/**/*.md

      - name: Check Broken Links
        uses: lycheeverse/lychee-action@v1
        with:
          args: --no-progress specs/**/*.md docs/**/*.md

      - name: Validate Document Structure
        run: |
          find specs docs -name "*.md" | while read f; do
            grep -q "^# " "$f" || echo "$f: missing h1"
            grep -q '```mermaid' "$f" || echo "$f: missing mermaid diagram"
            grep -q '^> \*\*Status:\*\*' "$f" || echo "$f: missing metadata header"
          done
