apiVersion: v1
kind: Service
metadata:
  name: vaeloom-ai-service
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: ai-service
    app.kubernetes.io/component: ai
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: ai-service
