apiVersion: v1
kind: Service
metadata:
  name: vaeloom-analytics-service
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: analytics-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3070
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: analytics-service
