apiVersion: apps/v1
kind: Deployment
metadata:
  name: vaeloom-rbac-service
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: rbac-service
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: rbac-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: rbac-service
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '3170'
    spec:
      containers:
        - name: rbac-service
          image: vaeloom/rbac-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3170
              name: http
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: vaeloom-config
                  key: NODE_ENV
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3170
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3170
            initialDelaySeconds: 5
            periodSeconds: 5
