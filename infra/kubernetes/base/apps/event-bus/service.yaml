apiVersion: v1
kind: Service
metadata:
  name: vaeloom-event-bus
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: event-bus
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3040
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: event-bus
