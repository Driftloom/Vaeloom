apiVersion: apps/v1
kind: Deployment
metadata:
  name: vaeloom-document-ingestion
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: document-ingestion
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: document-ingestion
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: document-ingestion
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '3110'
    spec:
      containers:
        - name: document-ingestion
          image: vaeloom/document-ingestion:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3110
              name: http
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: vaeloom-config
                  key: NODE_ENV
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3110
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3110
            initialDelaySeconds: 5
            periodSeconds: 5
