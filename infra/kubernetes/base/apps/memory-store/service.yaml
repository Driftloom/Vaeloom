apiVersion: v1
kind: Service
metadata:
  name: vaeloom-memory-store
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: memory-store
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3010
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: memory-store
