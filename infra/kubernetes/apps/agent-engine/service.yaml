apiVersion: v1
kind: Service
metadata:
  name: vaeloom-agent-engine
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: agent-engine
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3060
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: agent-engine
