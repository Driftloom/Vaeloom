apiVersion: v1
kind: Service
metadata:
  name: vaeloom-plugin-service
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: plugin-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3160
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: plugin-service
