apiVersion: v1
kind: Service
metadata:
  name: vaeloom-notification-service
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: notification-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3150
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: notification-service
