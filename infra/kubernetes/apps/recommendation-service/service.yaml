apiVersion: v1
kind: Service
metadata:
  name: vaeloom-recommendation-service
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: recommendation-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3180
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: recommendation-service
