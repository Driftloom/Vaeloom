apiVersion: v1
kind: Service
metadata:
  name: vaeloom-audit-service
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: audit-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: audit-service
