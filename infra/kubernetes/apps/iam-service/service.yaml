apiVersion: v1
kind: Service
metadata:
  name: vaeloom-iam-service
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: iam-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3120
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: iam-service
