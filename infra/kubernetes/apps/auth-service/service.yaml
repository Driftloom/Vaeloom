apiVersion: v1
kind: Service
metadata:
  name: vaeloom-auth-service
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3020
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: auth-service
