apiVersion: v1
kind: Service
metadata:
  name: vaeloom-billing-service
  namespace: vaeloom
  labels:
    app.kubernetes.io/name: billing-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3090
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: billing-service
