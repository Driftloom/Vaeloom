# PHASE A.2.1 — SECURITY CONTROL MATRIX

**Evaluation Principle:** Zero-Trust Execution Verification  
**Real Path Requirement:** Controls must be load-bearing on real execution
paths, not merely in unit test stubs.

---

| Control                   | Threat                                            | Enforcement Point                                                                                         | Real Path                                                                                                                                      | Test                                                                                                                         | Evidence                                                                                                                                                         | Status               |
| ------------------------- | ------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| **Tenant isolation**      | Cross-tenant data access                          | `TenantMiddleware` + RLS `p_users_tenant` + PostgreSQL `current_setting('app.tenant_id')`                 | Incoming HTTP request -> middleware extracts JWT tenant claim -> sets `TenantContext` -> runs `SET LOCAL app.current_tenant_id` -> SQL queries | `test_attack_1_cross_tenant_search_leak`, `test_target_vaeloom_rls_isolation`, `test_rls_live_pooling.py`                    | Cross-tenant queries return 0 rows; cross-tenant updates/deletes affect 0 rows; cross-tenant inserts fail with `InsufficientPrivilegeError` / `ProgrammingError` | **CLOSED**           |
| **Workspace isolation**   | Cross-workspace data access                       | RLS `p_*_workspace` + `check_user_workspace_access` + `TenantContext.workspace_id`                        | HTTP request -> `TenantMiddleware` sets workspace -> SQL queries filtered by RLS; background worker queries authorized via DB access table     | `test_attack_12`, `test_attack_14`, `test_attack_15`, `test_target_vaeloom_rls_isolation`, `test_rls_live_pooling.py`        | Foreign workspace rows invisible (0 rows); cross-workspace memory delete/read denied; pool connection reuse between workspace A and B verified                   | **CLOSED**           |
| **Search authorization**  | Data leak across workspaces or unauthenticated    | `search_service.py` fail-closed checks + RLS                                                              | `search_service.search_memories`, `search_documents` -> requires `workspace_id` -> executes DB search                                          | `test_attack_1`, `test_attack_3_unscoped_search_fail_closed`                                                                 | Empty or NULL workspace/tenant returns empty list or raises `ValueError`; no database query executed                                                             | **CLOSED**           |
| **Memory authorization**  | Memory tampering or data leak                     | `memory_service.py` ownership checks + RLS                                                                | `memory_service.get_memory`, `update_memory`, `delete_memory` checks `row.workspace_id == ctx_workspace_id`                                    | `test_attack_2`, `test_attack_14`, `test_attack_15`                                                                          | Cross-workspace memory delete raises 404/403; cross-workspace update denied                                                                                      | **CLOSED**           |
| **Approval integrity**    | Forged or tampered action approval                | `_canonical_payload_hash` SHA-256 + HMAC in `loop.py`                                                     | Orchestrator loop -> `consume_approval_for_action` matches canonical hash of proposed action payload against DB `agent_approvals.payload`      | `test_attack_6_approval_payload_swap`, `test_attack_16`                                                                      | Tampering with filename, action type, or args in payload causes hash mismatch and denies approval                                                                | **CLOSED**           |
| **Approval replay**       | Replay of previously consumed approval            | Atomic SQL update `UPDATE agent_approvals SET status = 'CONSUMED' WHERE id = :id AND status = 'APPROVED'` | Consequential agent execution -> single worker atomically transitions status                                                                   | `test_attack_7`, `test_attack_16`, `test_attack_25`                                                                          | Second execution attempt finds rowcount=0; re-prompts for approval (`request_approval`)                                                                          | **CLOSED**           |
| **Prompt quarantine**     | Injection / structural boundary breakout          | `quarantine()` in `prompt_compiler.py`                                                                    | Untrusted external / RAG data -> escaped within `<untrusted-data>` XML tags before LLM compilation                                             | `test_attack_10`, `test_attack_11`, `test_attack_30`                                                                         | `</untrusted-data>` tag injection escaped as `&lt;/untrusted-data&gt;`; structural breakout impossible                                                           | **CLOSED**           |
| **Tool authorization**    | Agent invoking unauthorized tool                  | `execute_tool` in `executor.py` checking `AgentCard.tools` and scopes                                     | Orchestrator / Agent -> `execute_tool()` -> checks `card_registry.get(agent_id)` -> raises `PermissionDeniedError`                             | `test_attack_4`, `test_attack_5`, `test_attack_28`                                                                           | Calling tool outside `card.tools` raises `PermissionDeniedError`; tool execution aborted before any handler runs                                                 | **CLOSED**           |
| **AgentCard enforcement** | Inactive or unpermitted agent capability          | `_dispatch_agent` in `loop.py` + `card_registry.py`                                                       | Dispatcher checks `card.status == 'ACTIVE'` before executing agent                                                                             | `test_attack_24_static_dispatch_agentcard_deactivation`                                                                      | Deactivated agent raises `PermissionError("Agent '...' is inactive")`                                                                                            | **CLOSED**           |
| **Background envelope**   | Forged or unsigned worker job                     | `background_envelope.py` + `queue_worker.py` + `background_daemon.py`                                     | BullMQ worker receives job -> `verify_background_envelope()` validates HMAC, expiry, nonce, payload hash -> verifies DB workspace membership   | `test_attack_17`, `test_attack_18`, `test_attack_19`, `test_attack_20`, `test_attack_21`, `test_attack_22`, `test_attack_23` | Unsigned jobs rejected; tampered envelopes rejected; expired envelopes rejected; replayed nonces blocked; non-member users denied                                | **CLOSED**           |
| **PostgreSQL RLS**        | Direct DB bypass under application role           | PostgreSQL RLS on 28 tables (`vaeloom` target DB)                                                         | Connection pool uses `vaeloom_app` role; queries subject to `p_*_workspace` / `p_*_tenant` policies                                            | `test_attack_26`, `test_target_vaeloom_rls_isolation`, `scratch/test_rls_live_pooling.py`                                    | Live PostgreSQL connection pooling verifies 0 rows without GUC, cross-workspace isolation, and `WITH CHECK` violation rejection                                  | **CLOSED**           |
| **Static dispatch**       | Consequential action execution without approval   | `_dispatch_agent` in `loop.py`                                                                            | Request routed through static agent (e.g. `OrganizationAgent`) -> file moves/renames require approval                                          | `test_attack_16`, `test_attack_25`                                                                                           | Generates proposals with `requires_approval=True`, returns `action="request_approval"`, blocks execution until approval consumed                                 | **CLOSED**           |
| **MCP**                   | Host escape / credential theft                    | Bounded configuration + AES-256-GCM encryption + write-approval gates                                     | MCP client runner -> checks interpreter allowlist -> encrypts secrets at rest -> requires approval for mutating tools                          | `test_attack_29_connector_secret_encryption`                                                                                 | Secrets encrypted with AES-256-GCM in DB; shell interpreters denied; residual risk formally bounded                                                              | **CLOSED (Bounded)** |
| **Auditability**          | Missing forensic evidence of unauthorized actions | `_audit_log` in `executor.py` + structured logging                                                        | Security violations emit structured audit logs with actor, tool, workspace, reason                                                             | Attacks 9, 28, executor logs                                                                                                 | Log entries contain `AGENT_CARD_DENIAL`, `WORKSPACE_TAMPER_ATTEMPT`, `missing_workspace_id`; no raw credentials logged                                           | **CLOSED**           |
