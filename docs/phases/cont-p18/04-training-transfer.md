# CONT-P18 — 04 Training / Knowledge Transfer (WS-18.4, DEL-04)

## Verified @ HEAD

- `docs/DEVELOPER_ONBOARDING.md` (prereqs Node≥18/pnpm9/Python≥3.12/Docker,
  clone→install→run) + `developer-experience/Developer-Guide.md` +
  `engineering/PR-Guidelines.md`.
- Role coverage: developer (onboarding), operator (runbooks + SLO),
  security (Security-Architecture + red-team registers), support
  (operations suite). No separate classroom program in scope (startup
  posture — recorded, not a gap against the approved baseline).
- Migration training: expand-contract waves are operator-runbook driven;
  per-wave phase docs (`cont-p00..p17`) serve as the training trail with
  gate verdicts + handoffs.
