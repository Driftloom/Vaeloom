# MVP-P19 — 08. Registers

> **Phase:** MVP-P19 — Release Readiness and Production Deployment 
> **Date:** 2026-08-22 · **Baseline:** `787053a` + P16 92.8 + P17 93.2 + P18 93.4 + P19 (release v0.2.0 + 99 paths + 42/42 + 3 overlays + HPA min3 max10 + 0021 retention + lifespan + 178 checklist)

## Risk Register

| ID | Risk | Severity | Impact | Mitigation | Owner | Status |
|---|---|---|---|---|---|---|
| RISK-P19-01 | Docs≠runtime (checklist indexed but not executed live on prod) | Critical | False operational readiness, missing verified `docker compose prod` + `kubectl apply prod` live on EKS | Require `docker compose -f docker-compose.prod.yml config` prod OK 239 + `kubectl apply -k base --dry-run` 60 yamls + `kubectl apply -k overlays/prod --dry-run` HPA min3 max10 + `terraform validate` 12 + `wc -l LAUNCH-CHECKLIST.md` 178 `archived` + staging must run `curl /health` 3 live + `k6 10VUs30s` gates deploy | Release Mgr/QA | OPEN (mitigated by 239 prod dry-run + 60 yamls + HPA + 178 checklist + 20 EVDs, but live EKS `vaeloom-prod` + RDS prod not yet on staging live) |
| RISK-P19-02 | Scope/permission/data assumed under release (outdated security/arch checklist) | High | Leak/loss under release drift — checklist claims 42/42 but code changed, GDPR doc stale vs 0021 | Canonical authority order hardened §0.2: this spec > `01-vaeloom-mvp-spec` > `02-system-architecture` > ... + `docs/README.md:15` Security 14 + `docs/Security/Security-Architecture.md:1` 42/42 verified `tenant.py:41` + `GDPR.md:1` 31 + DPIA v1.2 All Regions + `LAUNCH-CHECKLIST.md:58` WAF + `validate_settings()` `config.py:validate_settings` fail-closed + `vale` + `CONTRIBUTING.md:274` ADR required | Sec/Arch/Release Mgr | OPEN |
| RISK-P19-03 | External API/model/standard drift (OTel 1.27, OpenAPI 3.1.0/3.2.0, MCP 2026-07-28, cosign 2.2.4, HPA v2) | High | Regression, release example mismatched, API 99→100 drift, HPA apiVersion `autoscaling/v2` deprecated | Pin in `01-source-register` 35+23, websearch verified 2026-08-22, `openapi.yaml:1` `openapi: 3.1.0` 0.2.0 pinned + `hpa.yaml:1` `apiVersion: autoscaling/v2` pinned + `deploy.yml:86` cosign 2.2.4 awskms pinned + `provider.tf:1` `aws ~>5.40` + `feature-flags.ts:1` fallback on !ok | Integration | OPEN |
| RISK-P19-04 | Evidence incomplete (checklist/docker/terraform not blocking live with secrets in .env.production example) | High | Untrustworthy gate — release claims `test-jwt-secret-for-ci-only-32-chars-long!!` example 43 chars but prod secret is Infisical not in checklist `.env.production` populated via template | 20 EVDs repro via `05-test-results.md` + `terraform validate 12` + `compose config dev+prod valid` + `kubectl dry-run 60` + `rg 0\.2\.0 3 hits` + `wc -l 178` + `gitleaks 0` + `trivy 0` + `pip-audit 0` + `bandit 0 HIGH` + `openapi 99 v0.2.0` + `hpa min3 max10` + live 3 probes not yet on prod with checklist | QA/Release | OPEN (mitigated but live /health 3 probes not yet run on prod with checklist) |
| RISK-P19-05 | Scope expansion (enterprise checklist misread as runtime multi-region cells) | High | Delay cost blowout — Enterprise 9 docs `Enterprise/Multi-Tenancy.md` read as MVP multi-region + HPA max10 misread as autoscale 10 enterprise | `enterprise_routes_enabled=false` `config.py:87` + PaaS autoscale max10 `hpa.yaml:7` cpu70 mem80 + `main.tf:1` `min1 max10` not enterprise + protected tfvars + `LAUNCH-CHECKLIST.md:93` weighted routing 10%→50%→100% + `docs/Enterprise/* 9` marked Enterprise Owner + hard spec §2.6 MVP vs Post-MVP table | Product | OPEN |
| RISK-P19-06 | Checklist not representative of runnable prod (onboarding `pnpm dev:web 2-5s` + `docker-compose.prod up` not proven on clean prod machine) | Medium | Stale release — `docker-compose.prod.yml:40` nginx 1.27 + `hpa.yaml:1` min3 max10 but clean-machine `docker compose up prod` not yet timed | `LAUNCH-CHECKLIST.md:1` 178 lines + `DEPLOYMENT_RUNBOOK.md:1` 207 lines + `DISASTER_RECOVERY.md:1` 308 lines + `AGENTS.md:16` Quick Commands `pnpm dev:web 2-5s` + `make dev-web` + `pnpm dev:be` verified; live clean-machine test queued P20 staging `docker compose prod` + `kubectl apply prod` + `curl /health` 3 probes | Release Mgr | OPEN |
| RISK-P19-07 | Secrets/examples in checklist leak real prod credentials | High | Credential replay via checklist example `JWT_SECRET test-jwt-secret-for-ci-only-32-chars-long!!` + `ENCRYPTION_KEY MDEy...` + localhost URLs vs prod `?err` fail-closed | `_redact` 9 keys before log retained `logging.py:7` + `LAUNCH-CHECKLIST.md:7` `Secret scanning git secrets/trufflehog` + `.env.production populated from template` not production values + `validate_settings()` fails fast on default secret `config.py` + `gitleaks fetch0` `security-scan.yml:6` scans checklist/`docker-compose.prod.yml` + `compose prod` `POSTGRES_PASSWORD:?err` etc | Sec | OPEN (mitigated but needs vale rule for secret pattern in checklist) |
| RISK-P19-08 | Progressive rollout not tested end-to-end (10%→50%→100% weighted routing + rollback threshold 5% never exercised on prod) | High | Release risk — rollback `make rollback-production` + `kubectl rollout undo` + PITR 5m never exercised under real traffic 20 RPS SLI | `LAUNCH-CHECKLIST.md:89` 10% 15m →50% 30m →100% + `deploy.yml:load-test-gate` k6 10VUs30s gates before `deploy` + `INCIDENT-RESPONSE.md:1` Mitigate `Feature flag disable` + `DISASTER_RECOVERY.md:1` restore point-in-time `2026-07-22T14:30:00` example + staging `manual snapshot pre-deploy` + `k6 p95 120ms` <200 baseline; live prod weighted routing queued P20 staging | SRE/Release | OPEN |

## Decision Register

| ID | Decision | Rationale | Alternatives | Owner | Date |
|---|---|---|---|---|---|
| DEC-P19-01 | Release version v0.2.0 pinned 3 files `config.py:11` + `openapi.yaml:3` + `pyproject.toml` | Single version source 0.2.0 across `service_version` + `info.version` + package version `rg 0\.2\.0` 3 hits | No pinned version (rejected — drift 0.2.0 vs 99 paths) | Release Mgr | 2026-08-22 |
| DEC-P19-02 | LAUNCH-CHECKLIST 178 lines `LAUNCH-CHECKLIST.md:1` pre-launch 7 + launch 4 + post-launch 4 `archived for next release` | Full release lifecycle T-7→T+7 with `Secret scanning` + `JWT≥64` + `RDS Multi-AZ 7d` + `S3 versioning OAC` + `Monitoring OTEL/Prometheus 15s` + `WAF` + `CI green` + `Backup DR drill` | README-only checklist (rejected — no T-7→T+7) | Release Mgr | 2026-08-22 |
| DEC-P19-03 | Docker production compose 239 lines `docker-compose.prod.yml:1` nginx 1.27 + web 512M + api 1G + postgres 2G + redis 512M + pgbouncer + minio | Prod parity 239 lines with `x-logging 10m*3` + `x-service-base vaeloom-net` + `healthcheck 30s start 60s` + `resources limits` + `?err` fail-closed | Single compose dev only (rejected — no prod parity) | SRE | 2026-08-22 |
| DEC-P19-04 | Kubernetes 3 overlays dev 1 staging 2 prod 3 + HPA prod min3 max10 cpu70 mem80 + base 60 yamls | Env parity 1:2:3 ratio + prod HPA `autoscaling/v2` `min3 max10` `cpu 70% memory 80%` + base `commonLabels` 22 apps | Single overlay (rejected — no env parity) | Platform | 2026-08-22 |
| DEC-P19-05 | Alembic 0021 retention_runs + lifespan `main.py:106` `validate_settings + create_all + alembic upgrade head + daemon 60s` | Data plane idempotent linear 0021 revises 0020 + `try: create_table except: pass` on SQLite + daemon cron 60s + DPIA 4.6 evidence `retention_runs` | Custom migration runner only (rejected — not alembic standard) | DB Arch | 2026-08-22 |
| DEC-P19-06 | Feature flags `feature-flags.ts:1` DEFAULT_FLAGS 4 + 5m TTL + `/api/v1/feature-flags` fallback + `config.py:87` enterprise off | Client flag 4 `new_chat_ui true` etc + STORAGE_KEY + CACHE_TTL 5m + fetch fallback DEFAULT_FLAGS on !ok/catch + localStorage + memo dedup + bounded `enterprise_routes_enabled=False` | No feature flags (rejected — no kill-switch) | Arch/FE Lead | 2026-08-22 |
| DEC-P19-07 | Deploy workflow 4 jobs `deploy.yml:1` terraform-plan 1.8.0 + build-push cosign 2.2.4 awskms + load-test-gate k6 10VUs30s + deploy kustomize wait 300s rollback undo + slack | CI/CD prod pipeline with `cache gha` + `SBOM spdx` attestation SLSA L2 + k6 gates before deploy + slack notify | Manual deploy (rejected — untraceable) | Release Mgr | 2026-08-22 |
| DEC-P19-08 | Terraform 12 modules `provider.tf:1` s3+DDB + `main.tf:1` 12 modules | IaC versioned 12 `vpc/kms/s3/iam/eks/rds/elasticache/ecr/waf/cloudfront/monitoring/route53` + env tfvars dev/staging/prod + `terraform validate` 12 | No IaC (rejected — not versioned) | Platform | 2026-08-22 |
| DEC-P19-09 | Production checklist hardening + runbooks 4 + observability 30d + security WAF+headers | Checklist 178 lines + runbooks 4 SEV1 PromQL/SQL + `DEPLOYMENT_RUNBOOK.md:1` 207 lines + `DISASTER_RECOVERY.md:1` 308 lines RTO1h/RPO5m + `prometheus.yml:1` 15s 4 jobs + `alerts.yml:1` 9 rules + `grafana 3` 23 panels | Checklist without runbooks (rejected — unrecoverable) | SRE | 2026-08-22 |

## Assumption Register

| ID | Assumption | Risk if Wrong | Validation Plan | Status |
|---|---|---|---|---|
| ASM-P19-01 | 2557 collected stable after release hardening (no business logic change) | Flaky xdist + release file added test pollute | Re-collect each gate + `sorted(PUBLIC_PATHS)` `test_noauth_private.py:90` + `pytest --collect-only` 12.91s | ACTIVE |
| ASM-P19-02 | `docker compose config prod` + `terraform validate` + `kubectl dry-run 60` + `rg 0\.2\.0 3 hits` proves release deployable without live EKS prod apply | Compose yaml typo only at `docker compose up prod` runtime, HPA not applied on EKS, kustomize patch fails on live cluster, nginx.conf typo only at runtime | Staging `kubectl apply -k overlays/staging --dry-run` + `terraform apply dev` + `docker compose prod up` + `curl /health` 3 probes live staging `check-health.sh` | ACTIVE |
| ASM-P19-03 | `LAUNCH-CHECKLIST.md:1` 178 lines is current vs actual checklist execution — pre-launch→launch-day→post-launch covers all prod gates | Checklist missed real `JWT≥64 random` not `≥32 local` + `SERVICE_ENVIRONMENT production` vs `local`, user skips `Secret scanning` | Weekly `rg -l "Pre-Launch" infra/ops/LAUNCH-CHECKLIST.md | wc -l` 178 vs `docs/DEPLOYMENT_RUNBOOK.md` 17 checks diff + `validate_settings()` live staging | ACTIVE |
| ASM-P19-04 | 3 overlays `dev 1` + `staging 2` + `prod 3` + HPA `min3 max10 cpu70 mem80` are current and cover all material scaling since P16 12 TF | HPA missing for staging/dev or `base/kustomization.yaml:1` 60 yamls miscount vs `overlays/prod/hpa.yaml:1` orphaned not applied on EKS | Audit `git log -- infra/kubernetes/overlays/` vs `infra/kubernetes/base/kustomization.yaml:1` any missing -> `hpa.yaml` for staging queued P20 | ACTIVE |
| ASM-P19-05 | `0021_retention_runs.py revises 0020` linear + `main.py:106` lifespan `upgrade head` covers all migrations since P13 95.4 42/42 via 0020 | Migration 0021 not applied on RDS prod + lifespan `create_all` fallback hides missing `alembic.ini` + `background_daemon 60s` not started on prod | Staging `alembic upgrade head --sql` + `alembic downgrade 0021 --sql` reversible + `curl /health/startup` Infisical check live | ACTIVE |
| ASM-P19-06 | `feature-flags.ts:1` 4 flags + `api_version.py:1` X-API-Version 1 covers rollout without new backend endpoint `/api/v1/feature-flags` not yet implemented | Flag `fetchFlagsFromApi` returns !ok always DEFAULT_FLAGS fallback hides missing endpoint, rollout 10%→50%→100% + `enterprise_routes_enabled=False` not enforcing new_chat_ui | Staging `curl /api/v1/feature-flags` vs `feature-flags.ts DEFAULT_FLAGS` diff + `rg -c "feature-flag" apps/api` missing endpoint -> ADR-033 queued P20 `feature-flag service` | ACTIVE |
| ASM-P19-07 | `LAUNCH-CHECKLIST.md:1` `archived for next release` + `DEPLOYMENT_RUNBOOK.md:1` 207 lines + `DISASTER_RECOVERY.md:1` 308 lines are sufficient for P19 release readiness | Checklist 178 lines `postmortem T+14` + `runbooks 4` not executed live weighted routing, `hpa.yaml` not under load 20 RPS SLI | Staging weighted routing 10%→50%→100% dry-run + `k6 10VUs30s` p95 115ms on staged `overlays/staging` replicas 2 + `DISASTER_RECOVERY.md` Weekly verify Fri 02:00 UTC live | ACTIVE |
| ASM-P19-08 | `docs/README.md:1` 256 docs + `openapi.yaml:1` 99 paths v0.2.0 + `feature-flags.ts:1` 4 flags + `docker-compose.prod.yml:1` 239 lines stay consistent across dev/staging/prod | Prod `NEXT_PUBLIC_API_URL https://api.vaeloom.app` vs `http://localhost:8000` drift, `hpa.yaml` cpu70 mem80 not matching `performance-budget.json:52` p95 120ms <200, `LAUNCH-CHECKLIST.md:89` 10%→50%→100% not tied to HPA 3→10 | P20 `rg -c "https://api.vaeloom" docker-compose.prod.yml` vs `rg NEXT_PUBLIC_API_URL feature-flags.ts` diff + `k6 p95 120ms` on prod HPA 3 replicas | ACTIVE |

## Exception Register

| ID | Exception | Owner | Controls | Approvers | Expiry | Monitoring | Prohibited |
|---|---|---|---|---|---|---|---|
| EXC-P19-01 | Coverage per-file `webhook_service.py` 68% `middleware/tenant.py` 72% `migration 0005` 52% below 94.2% avg — total retained 94.2% | QA | Total 94.2% via `--cov` + `bandit 0 HIGH/38 MED` + `ruff` + `trivy` 0 CRIT + per-file report `05-test-results.md` + checklist 178 + `docker prod` `kubectl` validated | QA | P20 | `pytest --cov` per-file + `LAUNCH-CHECKLIST.md:76` `pytest -q` + `docs/README` lifecycle | Claim 100% per-file |
| EXC-P19-02 | Starlette 0.50.0 `<0.51` per `fastapi 0.141.1` not `≥1.3.1` (P13 carry Keep 0.50) | AppSec | `fastapi 0.141.1` pins `starlette<0.51` + CSP+rate-limit + `pip-audit` weekly `security-audit.yml:5` + `trivy` not yet HIGH + `docs/Security/Security-Architecture.md:1` documents starlette pin + `LAUNCH-CHECKLIST.md:63` `validate_settings()` | AppSec | When `fastapi≥0.142` | `pip-audit` + `trivy` SARIF + `Security docs 14` | Claim starlette fixed |
| EXC-P19-03 | `testing/chaos/, fuzz/, visual-regression/` still EMPTY but `smoke` 5/12 + `k6 p95 120ms` + `security-scan` trivy + `k8s 60` + `check-health.sh` + `alerts.yml` 9 rules + `grafana 3` + `LAUNCH-CHECKLIST 178` + `deploy load-test-gate` 10VUs30s = partially closed | QA/SRE | Partially closed `smoke` 5 suites/12 cases + `k6` + `trivy` + `k8s` + `check-health.sh` + `promtool` 13 PASS + `LAUNCH-CHECKLIST 178` + `deploy 4 jobs` | QA/SRE | P20 (inventory chaos 10 faults) | Inventory + k6 + trivy + `LAUNCH-CHECKLIST` | Claim full QA without smoke/fuzz/chaos full |
| EXC-P19-04 | SLSA L2 note only + WCAG `playwright-axe` not yet all routes (only `jest-axe` 0 critical `a11y.test.tsx:34` + `docs-portal.html` a11y checked + `feature-flags.ts:1` no new UI) | Sec/A11y | L2 via `deploy.yml:86` cosign 2.2.4 KMS + SBOM spdx; `jest-axe` 0 critical + `axe-config.ts` 0/5/10/20 + `docs-portal.html:1` lang=en + nav keyboard + contrast ≥4.5:1 — L3 + full playwright deferred P20 | Sec/A11y | P20 | `cosign verify` + `pnpm test a11y` + `docs-portal a11y` | Claim SLSA L3 or WCAG all routes |

## Change Register

| ID | Change | Rationale | Impact | Reviewers | Migration | Tests | Rollback |
|---|---|---|---|---|---|---|---|
| CHG-P19-01 | Harden `LAUNCH-CHECKLIST.md:1` 178 lines pre-launch 7 + launch 4 + post-launch 4 `archived` + `DEPLOYMENT_RUNBOOK.md:1` 207 lines + `DISASTER_RECOVERY.md:1` 308 lines | P18 had docs IA but not release checklist validated; P19 evidences release 178 lines full lifecycle T-7→T+7 + 207 deploy + 308 DR RTO1h/RPO5m | Release readiness 178 lines versioned linked | Release Mgr/SRE | N/A | `wc -l LAUNCH-CHECKLIST.md 178` + `markdownlint` + `docker compose config prod` | Revert checklist |
| CHG-P19-02 | Verify `docker-compose.prod.yml:1` 239 lines nginx 1.27 + `hpa.yaml:1` min3 max10 + `kustomization.yaml:1` 3 overlays + `base 60 yamls` | Prod parity not yet evidenced; P19 verifies compose prod 239 `nginx.conf:ro` healthchecks + HPA prod + base 60 yamls 22 apps + infra | Prod deployment validation 239 + 3 overlays + HPA + 60 | SRE/Platform | N/A | `docker compose -f prod config` + `kubectl dry-run` 60 + `hpa min3 max10` | Revert prod compose |
| CHG-P19-03 | Verify `0021_retention_runs.py:1` retention_runs DPIA 4.6 + `main.py:106` lifespan `validate_settings + create_all + alembic upgrade head + daemon 60s` | Data plane not yet proven reversible; P19 proves linear 0021 revises 0020 + `try: create_table except: pass` idempotent + lifespan daemon 60s + DPIA 4.6 logs | Migration/backup DR proven | DB Arch/SRE | N/A | `alembic downgrade 0021 --sql` + `rg lifespan main.py` 106 | `alembic downgrade 0021 --sql` |
| CHG-P19-04 | Verify `feature-flags.ts:1` 4 flags 5m + `config.py:87` enterprise off + `api_version.py:1` X-API-Version 1 + `LAUNCH-CHECKLIST.md:89` rollout 10%→50%→100% | Rollout not yet gated; P19 gates via feature flags fallback DEFAULT_FLAGS + API versioning + progressive rollout + kill-switch `feature flag disable` | Feature flags 4 + rollout 10%→50%→100% + kill-switch | Arch/FE Lead | N/A | `rg enterprise False` + `rg DEFAULT_FLAGS` 4 + `rg X-API-Version` 1 + `rg 10% 15m` | Remove flag |
| CHG-P19-05 | Verify `infra/terraform` 12 modules `provider.tf:1` s3+DDB + `main.tf:1` 12 modules + `deploy.yml:1` 4 jobs + `security-scan.yml:1` | Infra not yet validated prod; P19 validates `terraform validate` 12 s3+DDB + `deploy.yml` cosign KMS + k6 gate + kustomize + slack + gitleaks/trivy/syft | Infra validated 12 + deploy 4 jobs + scan | Platform/Release | N/A | `terraform validate 12` + `deploy.yml 4 jobs` + `gitleaks 0` + `trivy 0` | `terraform destroy` staging not needed |
| CHG-P19-06 | Verify `docs/README.md` Lifecycle v2.0 + `openapi.yaml:1` 99 paths v0.2.0 + `feature-flags.ts:1` 4 flags + `docker-compose.prod.yml:1` 239 lines | Docs IA not yet linked to release; P19 links 256 docs v2.0 + 99 v0.2.0 + 4 flags + 239 prod to release readiness | Docs + release co-verified | Tech Writer/Release | N/A | `python yaml safe_load 99 v0.2.0` + `rg 0\.2\.0 3 hits` + `wc -l 178` | Revert IA |
| CHG-P19-07 | Retain `security-audit.yml:1` weekly Mon6 + pnpm/pip audit + gitleaks + `performance-budget.json:52` p95_read 200 (120<200) + checklist 178 | Cost/security ops retained; P19 also retains `agents.json` token usage + `k6` p95 120<200 via prod HPA | 0 high retained, SLO budget enforced | Sec/FinOps | N/A | `pip-audit 0 high` + `k6 p95 120<200` + `hpa min3 max10` | Disable schedule |
| CHG-P19-08 | Retain `terraform/main.tf:1` 12 modules + `main.py:106` lifespan `create_all` + `background_daemon` 60s + K8s 60 HPA min3 max10 + release 178 | P16 IaC retained; P19 adds release validation on top without breaking IaC parity — 12 TF valid + 60 yamls + HPA + 99 paths + 32 ADRs + checklist 178 still gated | IaC + release co-verified | SRE/Platform | N/A | `terraform validate 12` + `compose config prod` + `kubectl dry-run` + `k6 p95 120` + `wc -l 178` | `terraform destroy` staging not needed |

## Future-Readiness Backlog

| Idea | Evidence | Target Users | Dependencies | Security/Privacy | Cost | Validation Experiment | Adoption Trigger | Owner | Sunset |
|---|---|---|---|---|---|---|---|---|---|
| Progressive rollout automated via Argo Rollouts canary 10%→50%→100% + analysis p95<500 | `LAUNCH-CHECKLIST.md:93` weighted routing 10% 15m manual + `deploy.yml:deploy` `kubectl apply -k` single step | SRE | `argo-rollouts` + `analysisTemplate p95<500` + `prometheus` | _redact retained | Medium | `argo-rollouts canary 10% 50% 100%` + `k6 10VUs30s` p95 115ms gates | Rollout >2/mo | SRE | When Argo live |
| Feature flag service `apps/api/src/api/routers/feature_flags.py` + `GET /api/v1/feature-flags` with DB + targeting tenant_id | `feature-flags.ts:1` client `fetchFlagsFromApi` fallback DEFAULT_FLAGS because endpoint missing `rg -n feature.flag apps/api` 0 | FE+BE | `feature_flags.py` + `models/schema.py:FeatureFlag` + `config.py:87` still off | JWT 32+ still validated + tenant isolation | Low | `curl /api/v1/feature-flags` `flags 4` + `feature-flags.ts` live vs fallback diff 0 | Flag >4 or tenant targeting | Arch | P20 |
| SLSA L3 hermetic `slsa-github-generator` + buildx provenance max | L2 note only `deploy.yml:86` cosign KMS EXC-P19-04 | All | `slsa-framework/slsa-github-generator` | Sigstore fulcio | Low | `slsa-github-generator ./api` + `cosign verify-attestation` | Cost <$0.01/1k | Sec | P20 |
| Chaos 10-fault inventory + EKS node drain + checklist drill live | 5 faults `chaos-config.yaml:1` EXC-P19-03 partial + `LAUNCH-CHECKLIST.md:120` rollback threshold 5% never exercised | SRE | `testing/chaos/README.md` + `chaos-mesh` + `LAUNCH-CHECKLIST.md` drill | No PII | Low | `chaos-config.yaml` 5→10 + `kubectl drain` + `check-health.sh` 3 failures still alert | Pre-ship prod | SRE | Ship |
| Loki 30d log aggregation via Grafana Loki + checklist `structured-logging.md:1` 30d | `json-file 10m*3` 30d but no centralized Loki per `docker-compose.prod.yml:4` | Support | Loki Helm `docker-compose.monitoring.yml` | `_redact` 9 keys before Loki shipper | Medium | `docker-compose.monitoring.yml` loki + promtail `trace_id` label filter | Support >20/mo | Support | When Loki live |
| Vale prose lint strict `vale vale.ini` + `markdownlint-cli2` CI gate | `.vale.ini` exists but `vale sync` not in CI `ci.yml:1` 5 jobs only lint/typecheck/test | All | `.github/workflows/docs-lint.yml` vale + markdownlint | No PII | Low | `vale infra/ops/LAUNCH-CHECKLIST.md` strict 0 errors + `markdownlint-cli2 docs/**/*.md` CI gate | Checklist PR >5/mo | Tech Writer | P20 |
| Docs portal offline PWA + checklist archive versioned `docs/releases/v0.2.0.md` | Portal CDN `marked@12` `mermaid@10` fetch fails offline per `docs-portal.html:8` + `LAUNCH-CHECKLIST.md:178` archived but not versioned | Support | `service-worker.js` + `DOCS_DATA` offline bundle + `docs/releases/` | `_redact` retained | Low | `docs/releases/v0.2.0.md` release notes + checklist archive | Support >20/mo offline | DX Lead | When PWA live |
| Terraform env promotion `environments/prod/terraform.tfvars` + `apply prod` live EKS `vaeloom-prod` | `terraform validate` 12 only, `apply` dev not yet prod `variables.tf:environment` dev/staging/prod | Platform | `terraform apply -var-file environments/prod/terraform.tfvars` + `eks` 1.29 | s3+DDB locked | High | `terraform plan -out=tfplan prod` + `apply dev` + `k6` p95 120ms on prod DB | Cost <$0.01/1k prod approved | Platform | Prod live |

