# MVP-P13 — 08. Registers

> **Phase:** MVP-P13 — Security, Privacy, and Compliance  
> **Date:** 2026-08-22 · **Baseline:** `0feb7ff`

## Risk Register

| ID                  | Risk                                                                                                                                                          | Severity | Impact                                                              | Mitigation                                                                                                                                                                                                              | Owner                            | Status                                                                   |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | ------------------------------------------------------------------------ |
| RISK-MVP-P13-01     | Docs mistaken for runtime completion                                                                                                                          | Critical | False readiness                                                     | Require runtime evidence/status labels per track-wide fixed decision; gate enforces evidence before pass                                                                                                                | Phase owner                      | OPEN — mitigated by VERIFIED labels + 233-test evidence                  |
| RISK-MVP-P13-02     | Scope/permission/data/compatibility assumed                                                                                                                   | High     | Leak/loss/rework                                                    | Block or reversible validated decision per §24; TenantMiddleware RLS + service-layer workspace filters on all queries                                                                                                   | Product/Architecture/Security    | OPEN — monitored, no critical blocker                                    |
| RISK-MVP-P13-03     | External API/model/standard changes                                                                                                                           | High     | Regression (MCP 2026-07-28, OWASP 2026, RFC 9700, provider pricing) | Pin versions in `01-source-register.md`; tests for OpenAPI 88 paths; owner + kill switch; provider keys via BYOK fallback chain                                                                                         | Integration/AI                   | OPEN — monitoring; pricing verified 2026-08-20                           |
| RISK-MVP-P13-04     | Evidence incomplete                                                                                                                                           | High     | Untrustworthy gate                                                  | Immutable reports (`05-test-results.md` bandit/pip-audit), baseline `0feb7ff`, traceability `07-evidence.md` EVD-P13-001..020                                                                                           | QA/Release                       | OPEN — mitigated by 20 EVD rows                                          |
| RISK-MVP-P13-05     | MVP scope expansion                                                                                                                                           | High     | Delay/complexity                                                    | Strict scope gate §5 Out of scope: Enterprise SSO/SCIM, institution admin, billing, marketplace, multi-region cells, cross-user memory, unsupported job automation — all gated behind `enterprise_routes_enabled=false` | Product                          | OPEN — deferred ideas logged in backlog (§ Future-Readiness)             |
| RISK-P12-10-carried | BYOK sends memory content to third-party provider under provider DPA                                                                                          | Medium   | Data processed per provider policy, not Vaeloom DPA                 | Documented in `06-security-privacy.md` + `AI-Governance.md`; consent-language review performed; user's provider agreement applies; 3 consent scopes enforce purpose                                                     | Security Lead / Privacy Engineer | OPEN — accepted with disclosure, P14 consent language DPO review pending |
| RISK-P12-09-carried | ENCRYPTION_KEY compromise exposes BYOK/provider keys                                                                                                          | High     | Keys decrypted, account misuse                                      | Key in SecretManager (Infisical), rotation path exercised in `test_provider_key_service.py`, keys per-workspace to limit blast radius                                                                                   | Security Lead                    | OPEN — monitoring                                                        |
| RISK-P13-06-new     | RLS enforced on 37/42 tables (5 via service filters: `users, agents, permissions, provider_keys, document_actions` not RLS) — was stale 4/36 claim fixed F-04 | High     | Service-layer bypass if filter omitted on those 5 tables            | `0010_rls_force_and_roles.py` 34 tables + `0019` adds 3 =37; `0019` now fail-closed after F-05 fix; 5 non-RLS tables audited via `rg workspace_id`                                                                      | IAM Engineer                     | OPEN — narrowed to 5 tables, P15 adds `permissions` if needed            |
| RISK-P13-07-new     | Adversarial detection regex-only (14 patterns) — sophisticated bypass possible                                                                                | High     | Prompt injection evasion                                            | Layer with LLM-based classifier planned P14 (per ADR), red-team with real attacks; 29 tests cover known patterns + base64/override                                                                                      | AI Safety Lead                   | OPEN — deferred to P14                                                   |
| RISK-P13-08-new     | Starlette CVEs (PYSEC-2026-161/248/249) — needs upgrade ≥1.3.1                                                                                                | Medium   | Dependency vulnerability                                            | Must-fix pre-production per `09-gate-report.md:60`; upgrade tracked as P14 task                                                                                                                                         | AppSec Engineer                  | OPEN — blocked on upgrade, not prod                                      |
| RISK-P13-09-new     | Input sanitization ADR-031 designed but not fully wired                                                                                                       | Medium   | Excessive agency via unsanitized tool params                        | Designed in `Tools/definitions.py`, partial in `executor.py`; full wiring deferred to P14 validation                                                                                                                    | AppSec Engineer                  | OPEN — P14                                                               |

## Decision Register

| ID             | Decision                                                                                                                                                                                     | Rationale                                                                                                                                                    | Alternatives Rejected                                                                               | Owner                        | Date       |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------- | ---------------------------- | ---------- |
| DEC-MVP-P13-01 | Keep `IPAllowlistMiddleware` conditional on `settings.ip_allowlist` (not always-mounted)                                                                                                     | Enterprise Hardening table says middleware EXISTS but NOT MOUNTED by default — intentional; mounting requires env decision + CIDR validation                 | Always-mounted with empty allowlist (would block all)                                               | Security Architect           | 2026-08-22 |
| DEC-MVP-P13-02 | Keep `TenantMiddleware` inner than `AuthMiddleware` (corrects RLS never-set CRITICAL 2026-08-21)                                                                                             | Auth must populate `request.state.tenant_id` before Tenant sets GUCs; `main.py:177` adds Tenant before Auth so Auth outer executes first (Starlette reverse) | Auth inner → `tenant_context` empty, RLS fails to 0 rows for all                                    | IAM Engineer                 | 2026-08-22 |
| DEC-MVP-P13-03 | RLS on 37 tables (34 via 0010 +3 via 0019) + service-layer workspace filters on remaining 5 (`users, agents, permissions, provider_keys, document_actions`) — corrected from stale 4/36 F-04 | 5 non-RLS tables are non-workspace or global; migration explosion risk; service filters cover + tests verify                                                 | 42-table RLS (migration complexity, performance hit of RLS on vectors)                              | IAM Engineer / Data Engineer | 2026-08-22 |
| DEC-MVP-P13-04 | 14-pattern regex + base64 + override for prompt injection (no LLM classifier yet)                                                                                                            | Zero latency, deterministic, no LLM cost, adequate for MVP; LLM classifier would add recursive risk + cost                                                   | LLM-based classifier (deferred to P14), third-party API (privacy dependency)                        | AI Safety Lead               | 2026-08-22 |
| DEC-MVP-P13-05 | 3 consent scopes (`data_processing`, `agent_access`, `email_marketing`)                                                                                                                      | GDPR purpose limitation minimal set; expanding scopes requires DPO review                                                                                    | Per-connector scopes (granular but complexity for MVP), single boolean consent (insufficient)       | Privacy Engineer             | 2026-08-22 |
| DEC-MVP-P13-06 | GDPR export 12 ALLOWED_TABLES (not all 36)                                                                                                                                                   | Minimization — only tables with personal data; others are projections/cache rebuildable per §17                                                              | Export all tables (excessive), no export (non-compliant)                                            | Privacy Engineer             | 2026-08-22 |
| DEC-MVP-P13-07 | Bandit B608 (38 MEDIUM) classified as false positives (no suppression `# nosec`)                                                                                                             | `text()` with bind params is parameterized — not injection; adding `# nosec` would hide real future B608; docs note instead                                  | Add `# nosec B608` on all 38 lines (masks signal)                                                   | AppSec Engineer              | 2026-08-22 |
| DEC-MVP-P13-08 | Carry P12 exceptions EXC-P12-03/04 as fixed via 0018 (memory_versions + document_chunks)                                                                                                     | 0018 provides durable tables, SQLite-compatible via `create_all`; no data loss risk as P12 was in-memory only                                                | Keep in-memory (unacceptable durability), force Postgres-only migration (breaks local SQLite tests) | Data Engineer                | 2026-08-22 |
| DEC-MVP-P13-09 | Keep enterprise routes gated behind `enterprise_routes_enabled=false` (`main.py:258`)                                                                                                        | Track-wide fixed decision: Enterprise SSO/SCIM, billing, marketplace, etc. out of MVP scope — must remain disabled                                           | Enable enterprise routes for testing (scope leak)                                                   | Program/Product              | 2026-08-22 |
| DEC-MVP-P13-10 | Use existing `docs/security/*` enterprise-quality docs as deliverables (not rewrite)                                                                                                         | 17 files already upgraded, versioned 2026-07-12/13, owned Security Team — reuse with phase linkage, not duplication                                          | Rewrite per-phase docs (drift, duplication)                                                         | Security Architect           | 2026-08-22 |

## Assumption Register

| ID         | Assumption                                                                                                | Risk if Wrong                                               | Validation Plan                                                                                                      | Status |
| ---------- | --------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ------ |
| ASM-P13-01 | SQLite test env (`tmp_path` + `NullPool` + MockVector) adequately mirrors Postgres RLS behavior           | RLS policies not exercised in CI                            | Run against real Postgres in P14/P16 (staging) with `alembic upgrade head` + isolation tests against PG              | ACTIVE |
| ASM-P13-02 | 27-byte JWT HMAC key warning (`InsecureKeyLengthWarning`) is test-only (local env `jwt_secret` 27 bytes)  | Weak key in prod would fail `validate_settings()`           | `validate_settings()` rejects <32 chars in non-local env; prod requires ≥32, rotation via `Security-Architecture.md` | ACTIVE |
| ASM-P13-03 | `mock_llm` + `mock_connector_test` fixtures represent real provider/connecter behavior for security tests | Tests pass but real providers leak                          | Live-provider eval + connector integration tests in P14/P17 with BYOK real keys                                      | ACTIVE |
| ASM-P13-04 | GDPR processor mapping (Anthropic/OpenAI BYOK under user's DPA) is accepted via consent scopes            | User disputes consent basis                                 | DPO review of DPIA + consent language in P14 before prod; rights workflows already test export/delete                | ACTIVE |
| ASM-P13-05 | Starlette CVE upgrade ≥1.3.1 is non-breaking for FastAPI 0.115.x                                          | FastAPI constraint `fastapi>=0.115.0,<0.116.0` may conflict | Verify via `pip-audit` + staging deploy in P14                                                                       | ACTIVE |
| ASM-P13-06 | Under-13 exclusion via policy (no tech enforcement) is sufficient for MVP                                 | COPPA violation if children sign up                         | Age gate + parental consent deferred to enterprise (track-wide fixed decision), launch cohort filtered               | ACTIVE |

## Exception Register (per §28 — owner, controls, approvers, expiry, monitoring, prohibited work)

| ID         | Exception                                                                                                                                                         | Owner              | Controls                                                                                                                    | Approvers                  | Expiry                                       | Monitoring                                                                | Prohibited Downstream                                |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | --------------------------------------------------------------------------------------------------------------------------- | -------------------------- | -------------------------------------------- | ------------------------------------------------------------------------- | ---------------------------------------------------- |
| EXC-P13-01 | RLS on 37/42 tables (5 via service filters: `users, agents, permissions, provider_keys, document_actions`) — was stale 4/36 fixed F-04; 0019 now fail-closed F-05 | IAM Engineer       | `0010` 34 + `0019` 3 =37 fail-closed; 5 non-RLS via service filter; 6 isolation tests + `test_noauth_private`               | Security Lead, Phase owner | P15 gate                                     | New queries `rg workspace_id` audit on 5 tables                           | Cross-tenant queries without filter on 5 tables      |
| EXC-P13-02 | IP allowlist conditional (always mounted via `main.py:188` no-op when empty) — was stale "NOT MOUNTED" fixed F-18; conditional is intentional                     | Security Lead      | `IPAllowlistMiddleware` always mounted, no-op when `ip_allowlist=""`; health/auth bypass paths; fail-closed when configured | Security Lead              | Production deploy decision                   | `main.py:188` always mounted, env var                                     | Prod without explicit allowlist decision when needed |
| EXC-P13-03 | Starlette CVEs unpatched (needs ≥1.3.1)                                                                                                                           | AppSec Engineer    | pip-audit flagged, not prod-deployed; no exploit path in MVP local SQLite env                                               | Engineering Lead           | P14 gate (must-fix)                          | `pip-audit` on every gate                                                 | Production without upgrade                           |
| EXC-P13-04 | Input sanitization ADR-031 partial                                                                                                                                | AppSec Engineer    | Designed in `tools/definitions.py`, partial in `executor.py`; 29 injection tests cover                                      | AppSec Engineer            | P14 gate                                     | `rg tool.*sanit` review, red-team P14                                     | New tools without sanitization review                |
| EXC-P13-05 | Regex-only adversarial detection (no LLM classifier)                                                                                                              | AI Safety Lead     | 14 patterns + base64 + override, 29 tests, `llm_validator.py` wired                                                         | AI Safety Lead             | P14 gate                                     | `agent_eval.py` adversarial cases, red-team                               | Claims of LLM-based detection                        |
| EXC-P13-06 | Under-13 via policy only (no age gate)                                                                                                                            | Privacy Engineer   | COPPA policy in `Compliance.md`, launch cohort 13+ via signup, DPO pending                                                  | Privacy Engineer, Legal    | Contingent on launch region decision (BQ-04) | Cohort screening, analytics `age_verification` flag                       | Child-directed marketing or school cohort            |
| EXC-P13-07 | CSRF token store in-memory (single-process, F-06) — fails on PaaS multi-worker                                                                                    | Security Architect | Dict `_token_store` + HMAC; TTL 3600s; TODO `main.py:232` Redis for multi-worker; health/auth SKIP correct                  | Security Architect         | P15 (or when PaaS workers >1)                | Multi-worker deploy test: `uvicorn --workers 2` + CSRF mutation 403 check | Prod multi-worker without Redis store                |

## Change Register

| ID         | Change                                                                      | Rationale                                          | Impact                                                                    | Reviewers                   | Migration                                 | Tests                                 | Rollout / Rollback                         |
| ---------- | --------------------------------------------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------------- | --------------------------- | ----------------------------------------- | ------------------------------------- | ------------------------------------------ |
| CHG-P13-01 | Mounted SCIM router (`main.py:266`) + settings permissions fix              | Predecessor carry (P11 SAML)                       | SCIM endpoint now live `/scim`                                            | IAM Engineer, Security Lead | None (existing `scim` tables)             | `test_saml.py` pass                   | Additive; rollback `include_router` remove |
| CHG-P13-02 | Verified `TenantMiddleware` ordering fix (`main.py:177` Tenant before Auth) | CRITICAL audit 2026-08-21 RLS never-set            | RLS now sets `app.tenant_id` transaction-scoped                           | IAM Engineer, QA            | None                                      | `test_tenant_isolation.py:6` pass     | Swap order, `rg TenantMiddleware`          |
| CHG-P13-03 | Added `/consent/scopes` to `PUBLIC_PATHS` (`auth.py:13`)                    | Public consent metadata must be fetchable pre-auth | Consent scopes discoverable without token                                 | Privacy Engineer            | None                                      | `test_privacy_flows.py:scopes` pass   | Remove path                                |
| CHG-P13-04 | `0018_graph_memory_end_to_end.py` (memory_versions + document_chunks)       | Fixes EXC-P12-03/04 durability                     | DB-backed versioning + chunk→embedding wiring, SQLite-compatible fallback | Data Engineer, SRE          | `alembic upgrade 0018` / `downgrade 0017` | `test_memory_versioning.py`           | Downgrade to 0017                          |
| CHG-P13-05 | `background_daemon.py` + `supervisor.py` additive                           | Proactive cron/watchers (P12→P13 hardening)        | Agent schedules polled 60s, Gmail 06:00 UTC, etc.                         | SRE, AI/ML Engineer         | None (existing `agent_schedules` table)   | Not yet gated — P14 integration tests | Stop daemon via `lifespan`                 |
| CHG-P13-06 | No destructive changes per `allow_destructive_changes=false`                | MVP-P13 is hardening/doc phase                     | All changes additive, reversible                                          | Phase owner                 | N/A                                       | `pytest -q` 2459 pass                 | N/A                                        |

## Future-Readiness Backlog (§ Future-Readiness Overlay — deferred with adoption triggers)

| Idea                            | Problem/Evidence        | Target Users    | Dependencies              | Security/Privacy/Data Implications | Cost      | Compatibility/Migration                               | Validation Experiment                | Adoption Trigger                                     | Owner            | Sunset/Rejection                                                 |
| ------------------------------- | ----------------------- | --------------- | ------------------------- | ---------------------------------- | --------- | ----------------------------------------------------- | ------------------------------------ | ---------------------------------------------------- | ---------------- | ---------------------------------------------------------------- |
| Threat modeling living          | Quarterly review needed | All             | P13 threat model baseline | STIDE drift if arch changes        | Low (doc) | None (additive)                                       | Review at P15/P17                    | Enterprise phase or major arch change                | Security Team    | Sunset if P21 periodic model adopted                             |
| Privacy impact living           | Workload changes        | All             | DPIA v1.0                 | Data categories drift              | Low       | Add columns to DPIA                                   | Validate via PEN-test                | New data category added                              | Privacy Engineer | —                                                                |
| Input sanitization ADR-031 full | Partial wiring          | All             | Tools registry            | Excessive agency if unsanitized    | Medium    | Wrap executor per tool                                | Red-team payloads                    | New tool added                                       | AppSec           | Enforce at P14                                                   |
| RLS 42-table                    | 5 via filters           | Enterprise      | Migration per table       | Performance hit on vectors avoided | Medium    | Per-table policy, backfill `workspace_id` on 5 tables | Staging PG + isolation load (37 now) | Scale beyond 10k users/tenant or any bypass in audit | IAM Engineer     | Reject if service filters proven sufficient at scale on 5 tables |
| IP allowlist always-on          | Conditional today       | Enterprise prod | VPC allowlist env         | Blocks if CIDR wrong               | Low       | Env var + deploy var                                  | Preflight test against allowlist     | Prod deploy in regulated env                         | Security Lead    | Enable when ADR-011 approved                                     |
