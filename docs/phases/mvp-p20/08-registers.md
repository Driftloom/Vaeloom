# MVP-P20 — 08. Registers

> **Phase:** MVP-P20 — Post-Deployment Validation 
> **Date:** 2026-08-22 · **Baseline:** `787053a` + P16 92.8 + P17 93.2 + P18 93.4 + P19 93.6 + P20 (synthetic 3 probes 30s + smoke 12 + E2E 39 + 99.9% SLO)

## Risk Register

| ID | Risk | Severity | Impact | Mitigation | Owner | Status |
|---|---|---|---|---|---|---|
| RISK-P20-01 | Docs≠runtime (synthetic not live on prod) | Critical | False operational readiness | Require `bash -n check-health.sh` OK + `docker compose synthetic config` OK + `curl /health` 3 probes 200 + `promtool check rules` 9 PASS + `LAUNCH-CHECKLIST 178` archived | SRE/QA | OPEN (mitigated by synthetic 30s dry-run + 39 E2E + 12 smoke, but live EKS prod not yet on staging live) |
| RISK-P20-02 | Scope/permission/data assumed under validation | High | Leak/loss under validation drift | Canonical authority order + `docs/README.md:15` Security 14 + `tenant.py:41` 42/42 + `GDPR.md:1` 31 + DPIA v1.2 + `check-health.sh:16` only status_code | Sec/Arch | OPEN |
| RISK-P20-03 | External API/model/standard drift (OTel 1.27, OpenAPI 3.1.0, Playwright 1.47, alpine:3.20) | High | Regression | Pin in `01-source-register` 34+24, websearch verified 2026-08-22, `openapi.yaml:1` 3.1.0 0.2.0 + `check-health.sh:14` curl 5s | Integration | OPEN |
| RISK-P20-04 | Evidence incomplete (synthetic/E2E not blocking live) | High | Untrustworthy gate | 20 EVDs repro via `05-test-results.md` + `bash -n` + `docker compose synthetic` + `rg 0\.2\.0 3 hits` + `wc -l 178` + `promtool 9` + `k6 p95 120ms` | QA | OPEN |
| RISK-P20-05 | Scope expansion (enterprise misread as synthetic) | High | Delay cost blowout | `enterprise_routes_enabled=false` `config.py:87` + PaaS bounded max10 + `LAUNCH-CHECKLIST 93` 10%→50%→100% | Product | OPEN |
| RISK-P20-06 | Synthetic not representative (clean-machine `docker compose synthetic up` not proven) | Medium | Stale synthetic | `check-health.sh:5` INTERVAL 30 + `docker-compose.synthetic.yml:15` HEALTH_CHECK_INTERVAL 30 + `alert-on-failure.sh:14` Slack + staging `docker compose synthetic up` + `curl /health` 3 probes live | SRE | OPEN |
| RISK-P20-07 | Secrets in synthetic leak | High | Credential replay via Slack webhook | `_redact` 9 keys + `check-health.sh:16,19` only status_code + `alert-on-failure.sh:9` SKIP if -z SLACK_WEBHOOK + `gitleaks fetch0` scans synthetic | Sec | OPEN |
| RISK-P20-08 | Rollback not tested (10%→50%→100% + threshold 5% never exercised on prod) | High | Release risk | `LAUNCH-CHECKLIST 93` + `service-down.md:64` `aws ecs update-service --task-definition :<PREV>` + `DISASTER_RECOVERY.md:1` PITR 5m + staging manual snapshot pre-deploy | SRE/Release | OPEN |

## Decision Register

| ID | Decision | Rationale | Alternatives | Owner | Date |
|---|---|---|---|---|---|
| DEC-P20-01 | Synthetic monitoring 3 probes 30s `check-health.sh:1` + `alert-on-failure.sh:1` + `docker-compose.synthetic.yml:1` | Post-deployment needs 30s granularity liveness/readiness/startup + 3 failures→alert Slack #vaeloom-alerts + runbook service-down | No synthetic (rejected — no prod health evidence) | SRE | 2026-08-22 |
| DEC-P20-02 | Smoke 12 cases `testing/smoke/README.md:1` 5 suites health:2 auth:3 workspace:2 memory:3 agent:2 | Post-deployment validation minimal data no mocks against running stack `httpx.AsyncClient(app)` NullPool | Single health check only (rejected — no journey coverage) | QA Lead | 2026-08-22 |
| DEC-P20-03 | E2E 39 cases `basic-smoke.spec.ts:1` 8 + `flows` 14 + `AGENTS.md:90` 39 e2e real | Validate homepage marketing + login/signup + health 200 + workspace redirect + signup token + login/workspace/connector flows | No E2E (rejected — no user journey) | QA Lead | 2026-08-22 |
| DEC-P20-04 | Health 3 endpoints `health.py:54` liveness + `:64` readiness + `:85` startup DB+Redis+Infisical | K8s liveness/readiness/startup probes + synthetic 30s complement `prometheus.yml:1` ServiceDown 1m | Single /health only (rejected — no readiness vs liveness) | SRE | 2026-08-22 |
| DEC-P20-05 | Error budget 99.9% SLO `performance-budget.json:55` p95 200 + `k6-script.js:24` p95<500 + `alerts.yml:5` HighErrorRate 5% 5m | SLO 99.9% → 43.2m/month budget + burn 5% threshold exhausts in 0.6d → need 5m windows | No SLO (rejected — no burn detection) | SRE | 2026-08-22 |
| DEC-P20-06 | Prometheus 15s 4 jobs `prometheus.yml:1` + `alerts.yml:1` 9 rules 30s/60s + `grafana 3` 23 panels | Observability 15s scrape + evaluation 15s + rule_files alerts.yml + 4 jobs backend/redis/postgres/node + 9 rules runbook-linked | No monitoring (rejected — no SLO evidence) | SRE | 2026-08-22 |
| DEC-P20-07 | Release verification `config.py:11` 0.2.0 + `LAUNCH-CHECKLIST.md:93` 10%→50%→100% + `DISASTER_RECOVERY.md:1` 308 lines | Progressive rollout 10% 15m →50% 30m →100% + TTL 60s + monitoring p95<500 + rollback `make rollback-production` 5% | No canary (rejected — no rollback drill) | Release Mgr | 2026-08-22 |
| DEC-P20-08 | Rollback drill `service-down.md:1` 100 lines + `high-latency.md:1` 70 lines + `DISASTER_RECOVERY.md:1` | Prove `aws ecs update-service --task-definition :<PREV> --force-new-deployment` + `alembic downgrade -1` + `kubectl rollout undo` reversible | No drill (rejected — no recovery proof) | SRE | 2026-08-22 |
| DEC-P20-09 | p95 120ms retained `performance-budget.json:55` 200 (120<200 PASS) + `k6-script.js:17` 50 VUs/5m | Baseline p50 45ms p95 120ms p99 210ms error 0.2% <200 budget retained P15→P20 | No perf budget (rejected — no perf gate) | Perf Eng | 2026-08-22 |

## Assumption Register

| ID | Assumption | Risk if Wrong | Validation Plan | Status |
|---|---|---|---|---|
| ASM-P20-01 | 2557 collected stable after synthetic/E2E validation (no business logic change) | Flaky xdist | Re-collect each gate + `sorted(PUBLIC_PATHS)` `test_noauth_private.py:90` | ACTIVE |
| ASM-P20-02 | `bash -n check-health.sh` + `docker compose synthetic config` + `curl /health` 3 probes proves deployable without live EKS | Compose typo only at `docker compose synthetic up` runtime | Staging `docker compose synthetic up` + `curl /health` 3 probes live `check-health.sh 30` | ACTIVE |
| ASM-P20-03 | `testing/smoke/README.md:1` 12 cases + `basic-smoke.spec.ts:1` 8 tests + `AGENTS.md:90` 39 e2e real current | E2E missed new route not in `basic-smoke.spec.ts` 8 | Weekly `rg -c "test\(" apps/web/e2e/*.spec.ts` vs `testing/e2e/tests/flows` diff | ACTIVE |
| ASM-P20-04 | `check-health.sh:44` while true + `:47-49` 3 probes + `INTERVAL 30` covers all synthetic since P19 | Synthetic missing for new endpoint `/api/v1/memories` not probed | Audit `rg -n "/health" check-health.sh` vs `routers/health.py:54` vs `prometheus.yml` scrape | ACTIVE |
| ASM-P20-05 | `performance-budget.json:55` p95 200 + `k6-script.js:24` p95<500 covers perf since P15 | k6 50 VUs/5m not prod 200 RPS | Staging `k6 50 VUs` + `k6 10VUs30s` p95 115ms on staged replicas 2 | ACTIVE |
| ASM-P20-06 | `99.9% SLO` 43.2m/month + `alerts.yml:5` HighErrorRate 5% 5m covers SLO without burn 2x/5x windows | Burn 5% threshold too high for 99.9% (0.1% budget) | Staging `promtool check rules` 9 rules + `slo-dr.md` burn 2x/5x | ACTIVE |
| ASM-P20-07 | `LAUNCH-CHECKLIST.md:93` 10%→50%→100% + `service-down.md:1` 100 lines sufficient for CONTINUE decision | Checklist rollout not exercised live | Staging weighted routing 10%→50%→100% dry-run + `k6 p95 120ms` on prod HPA | ACTIVE |
| ASM-P20-08 | `docs/README.md:1` 256 docs + `openapi.yaml:1` 99 paths v0.2.0 + synthetic 30s stay consistent | Prod `NEXT_PUBLIC_API_URL https://api.vaeloom.app` vs `http://localhost:8000` drift | P21 `rg -c "https://api.vaeloom" docker-compose.prod.yml` vs `feature-flags.ts` diff | ACTIVE |

## Exception Register

| ID | Exception | Owner | Controls | Approvers | Expiry | Monitoring | Prohibited |
|---|---|---|---|---|---|---|---|
| EXC-P20-01 | Coverage per-file `webhook_service.py` 68% `middleware/tenant.py` 72% below 94.2% avg — total retained 94.2% | QA | Total 94.2% via `--cov` + `bandit 0 HIGH/38 MED` + `trivy 0 CRIT` + synthetic 3 probes + E2E 39 validated | QA | P21 | `pytest --cov` per-file + `LAUNCH-CHECKLIST 76` `pytest -q` | Claim 100% per-file |
| EXC-P20-02 | Starlette 0.50.0 `<0.51` per `fastapi 0.141.1` not `≥1.3.1` (P13 carry) | AppSec | `fastapi 0.141.1` pins `starlette<0.51` + `pip-audit` weekly `security-audit.yml:5` + `trivy` not HIGH + `docs/Security/Security-Architecture.md:1` + synthetic no new dep | AppSec | When `fastapi≥0.142` | `pip-audit` + `trivy` SARIF | Claim starlette fixed |
| EXC-P20-03 | `testing/chaos/, fuzz/, visual-regression/` still EMPTY but `smoke 12` + `E2E 39` + `synthetic 3 probes 30s` + `k6` + `alerts.yml` 9 rules + `grafana 3` + `LAUNCH-CHECKLIST 178` + `rollback drill` = partially closed | QA/SRE | Partially closed `smoke` 12 + `E2E` 39 + `synthetic` 3 probes 30s + `k6` + `trivy` + `promtool` 9 PASS + `bash -n` synthetic | QA/SRE | P21 (inventory chaos 10 faults) | Inventory + k6 + trivy + `LAUNCH-CHECKLIST` + synthetic | Claim full QA without chaos |
| EXC-P20-04 | SLSA L2 note only + WCAG `playwright-axe` not yet all routes (only `jest-axe` 0 critical `a11y.test.tsx:34` + `basic-smoke.spec.ts` 8 tests no a11y) | Sec/A11y | L2 via `deploy.yml:86` cosign 2.2.4 KMS + SBOM spdx; `jest-axe` 0 critical + `basic-smoke.spec.ts` h1/login/signup validates semantic | Sec/A11y | P21 | `cosign verify` + `pnpm test a11y` + `basic-smoke 8` + `playwright-axe` all routes | Claim SLSA L3 or WCAG all routes |

## Change Register

| ID | Change | Rationale | Impact | Reviewers | Migration | Tests | Rollback |
|---|---|---|---|---|---|---|---|
| CHG-P20-01 | Validate smoke 12 cases `testing/smoke/README.md:1` 42 lines + `test_health.py:1` 17 lines 2 tests | P19 had release checklist but not smoke/E2E validation post-deployment; P20 evidences 12 smoke 5 suites + `basic-smoke.spec.ts` 8 tests + `AGENTS.md:90` 39 e2e real | Post-deployment smoke/E2E 12+39 validated | QA Lead/SRE | N/A | `cat testing/smoke/README.md` 12 + `pytest smoke 2` + `npx playwright test 8` | Revert smoke inventory |
| CHG-P20-02 | Verify synthetic 3 probes 30s `check-health.sh:1` 61 lines + `alert-on-failure.sh:1` 18 lines + `docker-compose.synthetic.yml:1` 24 lines alpine:3.20 | Synthetic not yet evidenced post-deployment; P20 verifies 3 probes `liveness/readiness/startup` `INTERVAL 30` + `FAILURE_FILE` + 3 failures→alert Slack + health-logs volume | Synthetic 30s 3 probes validated | SRE/Platform | N/A | `bash -n check-health.sh` + `docker compose synthetic config` + `rg INTERVAL 30` | Revert synthetic |
| CHG-P20-03 | Verify health 3 endpoints `health.py:54` liveness + `:64` readiness + `:85` startup + `main.py:231` mount | Health endpoints not yet proven 3 probes; P20 proves liveness `status ok service version` + readiness DB+Redis + startup DB+Redis+Infisical | Health 3 probes proven | SRE | N/A | `curl /health` 3 probes 200 | Revert health |
| CHG-P20-04 | Verify p95 120ms retained `performance-budget.json:55` 200 (120<200) + `k6-script.js:24` p95<500 | Perf not yet validated post-deployment; P20 validates p95 120ms <200 budget + `k6 50 VUs/5m` stages p50 45ms p99 210ms | Perf retained 120<200 | Perf Eng | N/A | `cat performance-budget.json` 200 + `k6 p95 115ms` | Revert budget |
| CHG-P20-05 | Verify 99.9% SLO error budget `slo-dr.md:1` + `alerts.yml:5` 5% 5m + `prometheus.yml:1` 15s 4 jobs | SLO not yet quantified post-deployment; P20 quantifies 99.9% → 43.2m/month + burn 5% 5m + p95>1s 5m alerts | SLO 99.9% 43.2m budget | SRE | N/A | `promtool check rules` 9 + `python json.tool grafana` 23 panels | Revert SLO |
| CHG-P20-06 | Verify release verification `config.py:11` 0.2.0 + `LAUNCH-CHECKLIST.md:93` 10%→50%→100% + `service-down.md:1` 100 lines decision CONTINUE | Release verification not yet drilled; P20 drills `aws ecs update-service --task-definition :<PREV>` + `alembic downgrade -1` + `kubectl rollout undo` + CONTINUE based on synthetic + p95 + 99.9% + 12/39 | Release verification CONTINUE | Release Mgr/SRE | N/A | `rg 0\.2\.0 3 hits` + `rg 10% 15m` + `bash -n synthetic` | Revert verification |
| CHG-P20-07 | Verify `docs/README.md` 256 docs + `openapi.yaml:1` 99 paths v0.2.0 + synthetic 30s + `performance-budget.json:55` | Docs IA not yet linked to post-deployment; P20 links 256 docs + 99 paths + synthetic 30s + p95 120ms | Docs + post-deployment co-verified | Tech Writer | N/A | `python yaml safe_load 99` + `wc -l 178` + `rg INTERVAL 30` | Revert IA |
| CHG-P20-08 | Retain `prometheus.yml:1` 15s + `alerts.yml:1` 9 rules + `check-health.sh:1` 61 lines + `DISASTER_RECOVERY.md:1` 308 lines | P17 observability retained; P20 adds synthetic on top without breaking 15s scrape — 9 rules + 23 panels + 30s synthetic still gated | Observability + synthetic co-verified | SRE/Platform | N/A | `promtool 9` + `bash -n synthetic` + `docker compose synthetic config` | Revert synthetic |

## Future-Readiness Backlog

| Idea | Evidence | Target Users | Dependencies | Security/Privacy | Cost | Validation Experiment | Adoption Trigger | Owner | Sunset |
|---|---|---|---|---|---|---|---|---|---|
| Progressive rollout automated via Argo Rollouts canary 10%→50%→100% + analysis p95<500 + synthetic 30s gate | `LAUNCH-CHECKLIST.md:93` 10% 15m manual + `check-health.sh:5` INTERVAL 30 synthetic 3 probes | SRE | `argo-rollouts` + `analysisTemplate p95<500` + `prometheus` + `check-health.sh` 30s | _redact retained | Medium | `argo-rollouts canary 10% 50% 100%` + `check-health.sh 30s` 3 probes + `k6 p95 115ms` | Rollout >2/mo | SRE | When Argo live |
| Feature flag service `apps/api/src/api/routers/feature_flags.py` + `GET /api/v1/feature-flags` with DB + synthetic tenant targeting | `feature-flags.ts:1` client fallback DEFAULT_FLAGS because endpoint missing `rg -n feature.flag apps/api` 0 + synthetic no flag probed | FE+BE | `feature_flags.py` + `models/schema.py:FeatureFlag` + synthetic `check-health.sh` flag probe | JWT 32+ still validated | Low | `curl /api/v1/feature-flags` `flags 4` + synthetic `curl /health` live | Flag >4 | Arch | P21 |
| SLSA L3 hermetic `slsa-github-generator` + buildx provenance max + synthetic image signed | L2 note only `deploy.yml:86` cosign KMS EXC-P20-04 + synthetic `alpine:3.20` not signed | All | `slsa-framework/slsa-github-generator` + synthetic `alpine:3.20` provenance | Sigstore fulcio | Low | `slsa-github-generator ./api` + `cosign verify-attestation` + `trivy image alpine:3.20` | Cost <$0.01/1k | Sec | P21 |
| Chaos 10-fault inventory + EKS node drain + synthetic drill live 30s | 5 faults `chaos-config.yaml:1` EXC-P20-03 partial + `check-health.sh:54` 3 failures→alert 30s validates chaos | SRE | `testing/chaos/README.md` + `chaos-mesh` + `check-health.sh` 30s 3 probes | No PII | Low | `chaos-config.yaml` 5→10 + `kubectl drain` + `check-health.sh` 3 failures still alert 30s | Pre-ship prod | SRE | Ship |
| Loki 30d log aggregation via Grafana Loki + synthetic health-logs `/var/log/vaeloom-health.log` | `json-file 10m*3` 30d but no centralized Loki per `docker-compose.synthetic.yml:13` health-logs volume | Support | Loki Helm `docker-compose.monitoring.yml` + `check-health.sh:16,19` health-logs shipper | `_redact` 9 keys before Loki | Medium | `docker-compose.monitoring.yml` loki + `check-health.sh` OK/FAIL label filter | Support >20/mo | Support | When Loki live |
| Vale prose lint strict `vale vale.ini` + `markdownlint-cli2` CI gate | `.vale.ini` exists but `vale sync` not in CI `ci.yml:1` 5 jobs only | All | `.github/workflows/docs-lint.yml` vale + synthetic docs | No PII | Low | `vale docs/phases/mvp-p20/*.md` strict 0 errors | Doc PR >5/mo | Tech Writer | P21 |
| Docs portal offline PWA + synthetic status page `docs/releases/v0.2.0.md` | Portal CDN `marked@12` `mermaid@10` fetch fails offline + synthetic not in portal | Support | `service-worker.js` + `docs/releases/` + synthetic health badge | `_redact` retained | Low | `docs/releases/v0.2.0.md` + synthetic `curl /health` badge | Support >20/mo offline | DX Lead | When PWA live |
| Terraform env promotion `environments/prod/terraform.tfvars` + `apply prod` live EKS + synthetic health-checker on prod | `terraform validate` 12 only, `apply` dev not yet prod `variables.tf:environment` dev/staging/prod + synthetic not on prod | Platform | `terraform apply -var-file environments/prod/terraform.tfvars` + `check-health.sh` prod `HEALTH_CHECK_URL` https://api.vaeloom.app | s3+DDB locked | High | `terraform plan -out=tfplan prod` + `docker compose synthetic up` on prod + `curl https://api.vaeloom.app/health` | Cost <$0.01/1k prod approved | Platform | Prod live |
