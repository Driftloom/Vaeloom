# Agents (populated in file 05 / file 08)

This package will contain the **specialist agents** (Organization, Memory, Resume,
ATS, Job Search, Gmail, Scheduler, …), each built on the shared harness in
`../orchestrator/`.

It is intentionally empty at the foundation phase (file 01). No agent logic exists yet.

See:

- `Docs/Engineering/Implementation/08-specialist-agents.md`
- `Docs/AI/Agent-Prompt-Specs/*`
